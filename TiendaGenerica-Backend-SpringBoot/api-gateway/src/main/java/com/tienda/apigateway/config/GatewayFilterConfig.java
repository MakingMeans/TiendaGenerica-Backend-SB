package com.tienda.apigateway.config;

// configuration class left intentionally blank; global filter is picked up via @Component
// GatewayValidationFilter is now a reactive GlobalFilter and is annotated with @Component
// no explicit bean definition is required anymore
public class GatewayFilterConfig {
}